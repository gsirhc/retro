{
  "name": "scramble-web-tests",
  "private": true,
  "description": "Playwright integration suite for the Scramble arcade emulator front end",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "coverage": "COVERAGE=1 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
