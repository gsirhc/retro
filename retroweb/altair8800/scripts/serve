#!/usr/bin/env bash
# Build the WebAssembly module and serve the browser front end at
# http://localhost:8000. No arguments. Needs the Emscripten SDK on PATH.
set -euo pipefail
cd "$(dirname "$0")/.."
exec make serve
