{
  "name": "ibmpcat-web-tests",
  "private": true,
  "description": "Playwright integration suite for the IBM PC/AT emulator front end",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "coverage": "COVERAGE=1 playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "monocart-coverage-reports": "^2.13.0"
  }
}
